services:
  - type: worker
    name: freetheai-checkin
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python checkin_multi.py
    envVars:
      - key: DISCORD_TOKENS
        sync: false
